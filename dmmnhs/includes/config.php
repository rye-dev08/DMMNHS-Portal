<?php
// NOTE: Live credentials removed. The Laravel app (project root) uses its own
// `.env` file for the local MySQL connection (`student_portal` DB).
// Set your own local values here if you still need to run the legacy PHP app.
define('DB_HOST', '127.0.0.1');
define('DB_NAME', 'student_portal');
define('DB_USER', 'root');
define('DB_PASS', '');

// Optional: application settings for production
// define('APP_ENV', 'production');
// define('APP_DEBUG', false);
?>
